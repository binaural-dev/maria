<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_form_invoice_add_fields_coutnry" model="ir.ui.view">
            <field name="name">numero de factura editable</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
                <field name="arch" type="xml">
                    <xpath expr="//field[@name='payment_reference']" position="after">
                        <field name="fee_period" attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund'))],'required':[('move_type','in',('out_invoice','out_refund'))]}" /> <!--attrs="{'invisible': [('state','in',('draft',))]}"/>-->
                        <field name="pay_soon" attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund'))]}"/>
                    </xpath>
                </field>
        </record>
    </data>
</odoo>

