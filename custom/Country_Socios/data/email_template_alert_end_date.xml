<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!--Alerta de vencimiento-->
        <record id="email_alert_end_date_partner" model="mail.template">
            <field name="name">Alerta de vencimiento del socio</field>
            <field name="email_from">
                ${('"%s" &lt;%s&gt;' % (object.company_id.name, ctx.get('email_from')))|safe}
            </field>
            <field name="subject">Su accion esta proxima a vencer, ${object.name}</field>
            <field name="email_to">${object.email|safe}</field>
            <field name="lang">${object.lang}</field>
            <field name="model_id" ref="model_res_partner"/>
            <field name="auto_delete" eval="True"/>
            <field name="body_html" type="html">
                <div>   
                    <div>
                        <h2>${(object.name)}</h2>
                        <p>Su accion numero ${object.action_number.number} esta proximo a vencer el dia ${object.end_date_partner.strftime('%d/%m/%Y')}</p>
                        <p>Recuerde ponerse en contacto con nosotros para tomar las acciones necesarias</p>
                    </div>
                    <hr/>
                       
                        <img t-att-src="data:image/png;base64,${ctx.get('email_alert_signature')}" />
                    
                    <div>
                        <div>${object.company_id.name}</div>
                        ${object.company_id.phone}
                        % if object.company_id.email:
                        | ${object.company_id.email}
                        % endif
                        % if object.company_id.website:
                        | ${object.company_id.website}
                        % endif
                    </div>
                </div>
            </field>
        </record>
    </data>
</odoo>  